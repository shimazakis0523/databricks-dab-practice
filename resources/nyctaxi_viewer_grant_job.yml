resources:
  jobs:
    nyctaxi_viewer_grant_job:
      name: nyctaxi_viewer_grant_job
      # スケジュールなし。trips_daily_gold の初回作成後、または
      # nyctaxi-viewers グループを作り直したときなどに手動 Run する。
      tasks:
        - task_key: grant_viewer_access
          notebook_task:
            notebook_path: ../notebooks/grant_viewer_access.py
